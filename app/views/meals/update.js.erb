$('.calendar').fullCalendar('removeMeals', [<%= @meal.id %>]);
$('.calendar').fullCalendar(
  'renderMeal',
  $.parseJSON("<%=j render(@meal, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');
